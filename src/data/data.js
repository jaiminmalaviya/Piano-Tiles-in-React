export default {
   tileRows: [],
   config: {
      width: Math.min(window.innerWidth, 400),
      height: window.innerHeight,
      rows: 4,
      cols: 4,
      speed: 2,
      interval: 20,
      defaultSpeed: 2,
      blackTiles: 2,
      probability: 0.7,
   },
}
